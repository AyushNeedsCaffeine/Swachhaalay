# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Data (large files - track with DVC or LFS if needed)
data/*.csv
data/*.parquet

# Models (large files)
ml/models/*.joblib
ml/models/*.pkl
ml/models/*.h5

# Superseded Run A scratch (trained on old 17-col dataset before regeneration) -
# ~246 MB of dead weight; metrics are captured in docs/TRAINING_LOG.md and the
# canonical artifacts live in ml/models/ (also untracked by the rule above).
ml/models_v1/

# Logs & outputs
logs/
*.log
outputs/
figures/

# Jupyter
.ipynb_checkpoints/

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db
